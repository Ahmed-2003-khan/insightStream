# This file marks `ingestion_pipeline` as a Python package, allowing its
# modules (such as youtube_loader) to be imported elsewhere in the project
# using standard dot-notation imports (e.g., from ingestion_pipeline.youtube_loader import ...).
